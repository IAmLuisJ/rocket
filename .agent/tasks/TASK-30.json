{
  "id": "TASK-30",
  "title": "Add webapp template: ESLint flat config",
  "category": "config",
  "description": "Create an ESLint flat config for the client template that includes typescript-eslint recommended rules, React hooks plugin, and react-refresh plugin for Vite HMR support.",
  "acceptanceCriteria": [
    "src/templates/webapp/client/eslint.config.js exists",
    "typescript-eslint recommended rules are enabled",
    "eslint-plugin-react-hooks is configured",
    "eslint-plugin-react-refresh is configured"
  ],
  "steps": [
    {
      "step": 1,
      "description": "Create eslint.config.js template",
      "details": "Create src/templates/webapp/client/eslint.config.js: import js from '@eslint/js'; import globals from 'globals'; import reactHooks from 'eslint-plugin-react-hooks'; import reactRefresh from 'eslint-plugin-react-refresh'; import tseslint from 'typescript-eslint'; export default tseslint.config({ extends: [js.configs.recommended, ...tseslint.configs.recommended], files: ['**/*.{ts,tsx}'], languageOptions: { ecmaVersion: 2020, globals: globals.browser }, plugins: { 'react-hooks': reactHooks, 'react-refresh': reactRefresh }, rules: { ...reactHooks.configs.recommended.rules, 'react-refresh/only-export-components': ['warn', { allowConstantExport: true }] } })",
      "pass": false
    },
    {
      "step": 2,
      "description": "Add ESLint dependencies to client package.json.tmpl",
      "details": "Ensure client package.json.tmpl (TASK-21) includes in devDependencies: eslint, @eslint/js, globals, eslint-plugin-react-hooks, eslint-plugin-react-refresh, typescript-eslint.",
      "pass": false
    }
  ],
  "dependencies": ["TASK-21"],
  "estimatedComplexity": "low",
  "technicalNotes": [
    "This is the standard ESLint config generated by create-vite for React+TypeScript projects"
  ]
}
